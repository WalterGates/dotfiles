---
# os:
#   editCommand: 'floaterm'

gui:
  showIcons: true
  theme:
    selectedLineBgColor:
      - bold
    selectedRangeBgColor:
      - bold

git:
  paging:
    colorArg: always
    pager: delta --dark --syntax-theme='Visual Studio Dark+' --paging=never
